import { test } from 'node:test'
import assert from 'node:assert'

// Phase 0 skeleton — proves the test runner is wired up.
// Real contracts start at T1 (test/state.test.js) in Phase 1.
test('test runner is wired', () => {
  assert.strictEqual(1 + 1, 2)
})
